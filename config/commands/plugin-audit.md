# Plugin Audit — Active Plugin Review

When this command is executed, inspect `~/.claude/settings.json` and summarize active plugin usage.

## Output
- List enabled plugins by group
- Flag plugins that appear redundant with local commands or skills
- Identify candidates for disabling in low-context sessions
- Keep recommendations focused on token and startup overhead, not security review
