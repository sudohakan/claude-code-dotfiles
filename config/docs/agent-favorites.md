# Agent Favorites

Use `~/.claude/teams/favorites.json` as the lightweight shortlist for common team creation.

## Purpose
- Reduce browsing overhead across the full agent library
- Keep a practical starter set for routine engineering, planning, and review work

## Maintenance
- Update the favorites list when a role becomes a routine choice
- Keep the file small and biased toward frequently used roles
